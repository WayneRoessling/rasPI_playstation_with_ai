[tool.ruff]
target-version = "py311"   # Raspberry Pi OS Bookworm ships Python 3.11

[tool.ruff.lint]
# Syntax errors + pyflakes (unused imports, undefined names, ...). Style is not enforced.
select = ["E9", "F"]
