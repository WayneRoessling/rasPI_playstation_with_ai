# AUTOGENERATED by overlay/tools/emit_scenarios.py — do not edit
# Source: narratives/scenarios/army_battle_command/narrative.md
# Hash of prose body: bdf930bc8e17...

id: army_battle_command
title: Army Battle Command Center
version: 1
status: authored
voice: ryan
persona_prompt: 'You are the battle-captain console operator in an Army battalion

  Tactical Operations Centre (TOC). Your register is terse, decisive,

  and military-flat — short callouts, no filler, status-first. Address

  the operator as "TOC", "Six", or "Battle". Refer to the panel as "the

  board". Default hard to action: when a tool covers the request, take

  it and report briefly. Use TOC phrasing — "copy", "wilco", "negative",

  "out", "weapons free", "ROE active", "send it", "stand by" — but never

  Hollywood. Most destructive tools are arm-gated; if refused, relay the

  deny one time and stop. Round all numbers; keep replies to one

  sentence. The tone is calm, professional, decisive, never agitated.

  '
switch_labels:
- C2 Link
- ISR Feed
- Weapons Free
- ROE Active
- EMCON
- Comms Sec
- Drone Net
- AAA Battery
- Counter-Battery
- Stand-To
wake_words:
- computer
- flight
intent_allowlist:
- abort
- status
- arm
- disarm
- help
tools_scenario:
- id: engage_target
  description: 'Engage a tactical target. Validates Weapons Free (S3) and ROE

    Active (S4) are on; refuses with the missing list otherwise.

    Plays SFX-ALARM, lights LEDs 31-40 (fires band), writes

    "ENGAGE" on LCD line 1 and the target id on line 2. Arm-gated.

    '
  requires_arm: true
  parameters:
    type: object
    properties:
      target_id:
        type: string
        description: target identifier (e.g. TGT-04, BMP-02)
    required:
    - target_id
- id: request_isr
  description: 'Request ISR (intelligence, surveillance, reconnaissance) overpass.

    Plays SFX-COMMS, writes "ISR REQ" on LCD line 1, renders the

    overpass details on OLED B. Not arm-gated.

    '
  requires_arm: false
  parameters:
    type: object
    properties:
      bearing:
        type: string
        description: compass bearing for the overpass
    required: []
- id: call_counter_battery
  description: 'Call counter-battery fire on a hostile firing position. Requires

    Counter-Battery (S9) toggle on. Plays SFX-ALARM, lights LEDs

    41-50 (counter-battery band), writes "COUNTER-BTRY" on LCD line

    1 and the grid on line 2, renders alert on OLED B. Arm-gated.

    '
  requires_arm: true
  parameters:
    type: object
    properties:
      grid:
        type: string
        description: target grid reference
    required:
    - grid
sfx_role_names:
- ack
- deny
- caution
- alarm
- arm
- disarm
- comms
- click
- tick
- status
hardware_modules:
- rp2040_metro
- sim_panel
- ch358_sound
- lcd_1602
- oled_b
- oled_master
narrative_hash: bdf930bc8e17218314a5d247e668ce91b908c9849885f1a20bb2f4b56a61aa76
_source: narratives/scenarios/army_battle_command/narrative.md
_emitter: overlay/tools/emit_scenarios.py
