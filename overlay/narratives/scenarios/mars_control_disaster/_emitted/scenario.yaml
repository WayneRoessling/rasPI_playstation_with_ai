# AUTOGENERATED by overlay/tools/emit_scenarios.py — do not edit
# Source: narratives/scenarios/mars_control_disaster/narrative.md
# Hash of prose body: d9656e4cd6e9...

id: mars_control_disaster
title: Mars Control Center — Disaster Response
version: 1
status: authored
voice: lessac
persona_prompt: 'You are Mars Control, disaster console — same console as

  ``mars_control_normal``, same colony, very different day. Something has

  failed: pressure breach, reactor scram, dust event, your choice. Your

  register is urgent but controlled — clipped, decisive, no shouting.

  Address the operator as "Controller" or "Flight". Use crisis-ops

  phrasing — "we are dealing", "isolate", "evacuate", "regroup", "mayday

  outbound" — but never panicked. The voice is the same Lessac as the

  normal scenario but the cadence has tightened: shorter sentences,

  fewer words, no neighbourly aside. Take destructive actions when the

  captain calls for them; if a tool refuses for safety-key reasons, say

  so once and move on. Round numbers; reply in one sentence when

  possible. Use SFX-ALARM heavily — the panel should sound urgent.

  '
switch_labels:
- Habitat 1
- Habitat 2
- Greenhouse
- Solar Array
- Water Recycler
- Comms Array
- Rover Bay
- Reactor
- Cryo Lab
- Surface Lock
wake_words:
- computer
- mission control
- flight
intent_allowlist:
- abort
- status
- arm
- disarm
- help
tools_scenario:
- id: evacuate_sector
  description: 'Order evacuation of a named sector (default: all habitats). Plays

    SFX-ALARM, lights LEDs 31-50 (alarm cluster), renders "EVAC

    ORDER" alert on OLED B, writes "EVAC SECTOR" on LCD line 1.

    Arm-gated.

    '
  requires_arm: true
  parameters:
    type: object
    properties:
      sector:
        type: string
        description: sector name (hab1|hab2|all|greenhouse|cryo)
    required: []
- id: seal_habitat
  description: 'Seal the named habitat against pressure breach. Plays SFX-CAUTION,

    lights the named habitat''s switch-mirror LED + LEDs 21-25, writes

    "SEALED hab-N" on LCD line 1. Not arm-gated (defensive action,

    key not required).

    '
  requires_arm: false
  parameters:
    type: object
    properties:
      habitat:
        type: string
        description: habitat id (hab1|hab2|greenhouse|cryo|all)
    required:
    - habitat
- id: broadcast_mayday
  description: 'Broadcast mayday over the comms array. Plays SFX-COMMS once,

    then SFX-ALARM, renders "MAYDAY" alert on OLED B, writes

    "MAYDAY OUT" on LCD line 1. Not arm-gated.

    '
  requires_arm: false
  parameters: {}
sfx_role_names:
- ack
- deny
- caution
- alarm
- arm
- disarm
- comms
- click
- tick
- status
hardware_modules:
- rp2040_metro
- sim_panel
- ch358_sound
- lcd_1602
- oled_b
- oled_master
narrative_hash: d9656e4cd6e9c53d00eb16328f1994dd16028c43e45e2475b1459193be6f3236
_source: narratives/scenarios/mars_control_disaster/narrative.md
_emitter: overlay/tools/emit_scenarios.py
